# Failure Modes

Currently we are not validating data coming from localStorage. If that data is corrupted, it could cause problems.

We aren't doing it because to heck with someone that would change that intentionally, and time is money. But if we start getting errors, maybe do it?
